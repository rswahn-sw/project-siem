---
- name: Install Nginx using native apt module
  apt:
    name: nginx
    state: present

- name: Ensure Nginx is started and enabled on boot
  service:
    name: nginx
    state: started
    enabled: yes

- name: Allow HTTP traffic through UFW (Firewall)
  ufw:
    rule: allow
    port: '80'
    proto: tcp